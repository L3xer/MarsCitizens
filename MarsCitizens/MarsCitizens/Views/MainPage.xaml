<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MarsCitizens.Views.MainPage"
             BindingContext="{Binding MainViewModel, Source={StaticResource Locator}}">
      <Label Text="{Binding CitizienCount}" VerticalOptions="Center" HorizontalOptions="Center" />

  <StackLayout IsVisible="{Binding IsBusy}"
               Padding="32"
               AbsoluteLayout.LayoutFlags="PositionProportional"
               AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1"
               BackgroundColor="#90000000">
    <ActivityIndicator IsRunning="{Binding IsBusy}" Color="#3CB9A8"/>   
  </StackLayout>
</ContentPage>